pragma solidity ^0.8.0;

contract Tree {
    /// @notice Exchanged `q1` `@tokenName(t1)` for `q2` `@tokenName(t2)`
    function exchange(uint256 q1, address t1, uint256 q2, address t2) external {
        // exchange q1 of t1 for q2 of t2
    }
}